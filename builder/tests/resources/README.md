These archive were created by running `cargo package` inside the following examples:
- `rocket/hello-world`
- `rocket/secrets` - remove the `Secrets.toml` from the `.gitignore` file
- `rocket/workspace`
- `next/hello-world` - with `--target wasm32-wasi `
