[package]
name = "archiving-test"
version = "0.1.0"
edition = "2021"

[dependencies]
# This test crate is never compiled, so bumping these is not needed
axum = "0.6.18"
shuttle-axum = "0.25.0"
shuttle-runtime = "0.25.0"
tokio = "1.28.2"
