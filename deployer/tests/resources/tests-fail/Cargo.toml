[package]
name = "tests-fail"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[workspace]

[dependencies]
rocket = "0.5.0-rc.2"
shuttle-service = { path = "../../../../service", features = ["web-rocket"] }
