[package]
name = "sleep-async"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[workspace]

[dependencies]
tokio = { version = "1.22.0", features = ["time"] }
shuttle-service = { path = "../../../" }
