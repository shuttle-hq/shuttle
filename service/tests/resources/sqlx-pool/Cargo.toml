[package]
name = "sqlx-pool"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[workspace]

[dependencies]
shuttle-service = { path = "../../../" }
shuttle-shared-db = { path = "../../../../services/shared-db", features = ["postgres"] }
sqlx = { version = "0.5", features = [ "runtime-tokio-native-tls" ] }
