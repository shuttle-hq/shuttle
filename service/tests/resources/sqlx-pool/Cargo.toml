[package]
name = "sqlx-pool"
version = "0.1.0"
edition = "2021"

[lib]

[workspace]

[dependencies]
shuttle-service = { path = "../../../", features = ["sqlx-postgres"] }
sqlx = { version = "0.5", features = [ "runtime-tokio-native-tls" ] }
